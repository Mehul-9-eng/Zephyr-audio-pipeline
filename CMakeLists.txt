cmake_minimum_required(VERSION 3.20.0)
find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(audio_pipeline_proto)

target_sources(app PRIVATE
    src/main.c
    src/audio_pipeline.c
    src/capture_backend.c
    src/sink_backend.c
)
